**** Project Notes ****

Merges branch 'master' of https://github.com/stazman/banking-api



**** Stubs ****




**** Notable Commits/To be written in Github comments ****

Merges branch 'master' of https://github.com/stazman/banking-api:

	Merged local files with remote repo after failed amend left files untracked on remote



**** Unused Code ****




ADMINUSERPOSTGRES
main method for testing
public static void main(String[] args) {
		
		
		AdminUserPostgres aup = new AdminUserPostgres();
		
		User u = new User();
		
		u.setFirstName("John");
		
		Integer regUser = aup.registerUser(u.getEmail());
		
	    System.out.println(regUser);
	    
		
	}
	
	

USERDAO

//	public Set<User> getUsersByRole(User r);??? do we need to include???
//	public Integer createUser (User u);
//	public String getUserByEmail(String email);
//	public String getUserByLastName(User lastName);
//public boolean deleteUser (User u);
//public String getRoleByUser (Role r);


USERSERVICE

//public boolean deleteUser (User u);
//public String getRoleByUser (Role r);	
//public User logIn(String username, String password) {
//	User u = userDao.getPersonByName(username);
//	if (password == u.getPassword())
//		return u;
//	else
//		return null;		
//}


	//not register
	//login
	//can update their own info if the id provided matches the id of the current user
	
	//owned accounts:
		//withdrawal D
		//deposit D
		//transfer between all accounts of user D
		//transfer between accounts owned by different users ... 
				//if the source account belongs to the current user D
		//can find accounts if the account belongs to the current user D
		//can find accounts by user if the id provided matches the id of the current user ??? D
		//can submit an account D
		//if the account belongs to the current user D
	
